syntax = "proto3";

package usercervice;

message GetUserRequest {
    string username = 1;
}

message GetUserResponse {
    string message = 1;
}

service UserService {
    rpc GetUser (GetUserRequest) returns (GetUserResponse);
}